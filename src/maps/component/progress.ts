import { MenuMap } from "./types";

export const progressMenuMap: MenuMap[] = [
    {
        label: "Usage",
        href: "#usage",
    },
];

export const progressCode = {
    usage: `import Progress from "@/components/ui/Progress/Progress";
    
<Progress value={20} />`,
};
